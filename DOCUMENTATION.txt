================================================================================
                    CHESS GAME PROJECT - COMPLETE DOCUMENTATION
================================================================================

PROJECT OVERVIEW
================================================================================
This is a fully functional chess game built using Python and Pygame. The project
implements a complete chess engine with board representation, piece movement 
logic, check detection, castling, and move validation. The game features:
- Interactive 8x8 chess board UI
- Full piece movement and capture mechanics
- Check detection and validation
- Castling support
- Move history and backtracking (Ctrl+Z)
- Visual highlighting of available moves

PROJECT STRUCTURE
================================================================================
chess/
├── chess.py                    # Main game file (all game logic)
├── DOCUMENTATION.txt           # This file
├── README.md                   # Project title
└── assests/
    ├── chessboard3.png         # Chess board background image
    └── chess_pieces.png        # Sprite sheet with all piece images


================================================================================
BOARD REPRESENTATION & PIECE ENCODING
================================================================================

BOARD STRUCTURE:
- The board is an 8x8 2D array (list of lists) called "board"
- board[i][j] represents the cell at row i, column j
- Rows 0-7 represent ranks 1-8 from bottom to top (in traditional chess notation)
- Columns 0-7 represent files a-h from left to right

PIECE ENCODING SYSTEM:
Positive numbers = Black pieces
Negative numbers = White pieces
0 = Empty cell

Piece Values:
- Queen:  1 (black) / -1 (white)
- King:   2 (black) / -2 (white)
- Rook:   3 (black) / -3 (white)
- Knight: 4 (black) / -4 (white)
- Bishop: 5 (black) / -5 (white)
- Pawn:   6 (black) / -6 (white)

EXAMPLE:
board[0][0] = 3  means Black Rook at top-left corner
board[7][0] = -3 means White Rook at bottom-left corner
board[3][3] = 0  means Empty cell

BOARD DIMENSIONS:
- Visual board size: 500 x 500 pixels
- Cell size: 62.5 x 62.5 pixels
- Offset (margin): 20 pixels around the board
- Piece images in sprite sheet: 60 x 60 pixels each


================================================================================
GLOBAL VARIABLES & DATA STRUCTURES
================================================================================

moves = []
  - Stores move history for backtracking functionality
  - Format: [(piece_value, old_location, new_location), ...]
  - Extended format for captures: (..., captured_piece_value)

kingloc = [[7,4], [0,4], 'wnotmoved', 'bnotmoved']
  - kingloc[0] = [row, col] of white king
  - kingloc[1] = [row, col] of black king
  - kingloc[2] = 'wnotmoved' or 'wmoved' (for castling validation)
  - kingloc[3] = 'bnotmoved' or 'bmoved' (for castling validation)

isrookmoved = ['wlnotmoved', 'wrnotmoved', 'blnotmoved', 'brnotmoved']
  - Tracks whether each rook has moved (used for castling)
  - wlnotmoved/wlmoved = white left rook (at row 7, col 0)
  - wrnotmoved/wrmoved = white right rook (at row 7, col 7)
  - blnotmoved/blmoved = black left rook (at row 0, col 0)
  - brnotmoved/brmoved = black right rook (at row 0, col 7)

flag = []
  - Tracks if a king is in check
  - Format: ['check', [king_row, king_col]] when a king is in check
  - Empty when no check is active

pins = {}
  - Tracks pinned pieces (pieces that can't move because moving would expose king)
  - Format: {(pinned_piece_row, pinned_piece_col): (pinning_piece_row, pinning_piece_col)}
  - Example: {(3,4): (0,4)} means piece at (3,4) is pinned by piece at (0,4)

next_moves = []
  - List of valid squares the selected piece can move to
  - Format: [(row, col), (row, col), ...]
  - Populated by calnextmoves() function
  - Cleared after each move

change_peice = [piece_value, [row, col]]
  - Stores which piece is currently selected for movement
  - change_peice[0] = the piece value (e.g., 1 for black queen)
  - change_peice[1] = the location [row, col] of the piece

turn = -1 or 1
  - -1 = White's turn
  - 1 = Black's turn
  - Switches after each move


================================================================================
                                  MAIN
================================================================================
-->INITIALIZATION:
  # numbers(): func print numbers
  # initial(): func initializes the board
  # peipeicelocsprite(): helps in organising data like which val pointes to which coordinates in the spritesheet of peices
  # load_board(): this func loads board based on the board VARIABLE
  # main while loop runs as gameloop and constantly checks for mouse clicks and key presses

--> CLICK:
  #it highlights current cell then
  #(initially) if you pressed on a peice: then it will call "calnextmoves" to calculate that peices next moves if it is its turn
  --> CALCULATE NEXT MOVES (calnextmoves):
    #if given peice is a PAWN: then it add its front 2 pos and daigonal capture to "next_moves" variable if possible
    #if given peice is KNIGHT: then it shows from a list of offsets which will return points when added with current position into "next_moves"
    #if it is QUEEN , ROOK OR BISHOP: then checks for valid directions for them and add valid points in those directions into "next_moves"

--> PINS:
  #checks if clicked peice is a QUEEN ROOK OR BISHOP then only pin is possible and checks does it exists if it exist add it to pins variable
  #after it check everytime does all elements in pins are still pinned or not if not then remove it


================================================================================
KNOWN LIMITATIONS & FUTURE IMPROVEMENTS
================================================================================

NOT YET IMPLEMENTED:
- Pawn promotion to queen/rook/bishop/knight
- En passant capture
- Castling movement (detection works, but pieces don't actually move)
- Checkmate detection
- Stalemate detection
- Threefold repetition
- 50-move rule
- Move validation during check for all edge cases
- Elo rating or AI opponent

POTENTIAL IMPROVEMENTS:
- Add AI opponent (minimax with alpha-beta pruning)
- Implement complete rule set (promotion, en passant, etc.)
- Add move history display
- Add timer for timed games
- Add save/load game functionality
- Improve graphics and animations
- Add sound effects
- Network multiplayer support

PERFORMANCE:
- Current implementation is efficient for single player
- Board state stored in 2D list (easy to modify)
- Move calculation runs in real-time without noticeable lag
- Rendering at 60 FPS is smooth on modern systems


================================================================================
CODE ORGANIZATION SUMMARY
================================================================================

SECTION 1: IMPORTS & INITIALIZATION (Lines 1-45)
- Import pygame and PIL
- Define piece encoding comments
- Initialize pygame and load images
- Calculate board dimensions

SECTION 2: RENDERING FUNCTIONS (Lines 45-80)
- numbers(): Draw coordinate labels
- initial(): Set up starting position
- load_board(): Render board and pieces
- highlight(), rectangle(), draw_circle(): Visual indicators

SECTION 3: UTILITY & DIRECTION (Lines 80-130)
- tellsign(): Get piece color
- line(): Get squares in direction
- direction(): Calculate direction between points
- banned_pos(): Find squares under attack

SECTION 4: MOVE CALCULATION (Lines 130-300)
- calnextmoves(): Main move calculation engine
- Handles all piece types
- Detects checks and pins
- Validates moves

SECTION 5: SPECIAL MOVE DETECTION (Lines 300-350)
- valid_points_in_check(): Moves during check
- ispinning(): Detect pinned pieces
- canitcastle(): Check castling validity

SECTION 6: USER INTERACTION (Lines 350-450)
- click(): Handle mouse clicks
- Piece selection and movement
- Turn management
- Check detection

SECTION 7: BACKTRACKING (Lines 450-470)
- backtrack(): Undo moves

SECTION 8: MAIN GAME LOOP (Lines 470-500)
- Game initialization
- Event handling
- Display update
- FPS control


================================================================================
HOW TO EXTEND THE PROJECT
================================================================================

TO ADD PAWN PROMOTION:
1. In click() function, detect when pawn reaches rank 8 or rank 1
2. Show UI to select promotion piece (queen, rook, bishop, knight)
3. Replace pawn value with selected piece

TO ADD CHECKMATE DETECTION:
1. After move detection, check if opposing king is in check
2. Calculate all possible moves for opposing side
3. If no legal moves and king in check = checkmate
4. Display message and stop game

TO ADD AI OPPONENT:
1. Implement minimax algorithm with board evaluation
2. After player move, call AI function
3. AI returns best move
4. Apply move to board
5. Switch turn back to player

TO ADD GAME TIMER:
1. Add clock object tracking game time
2. Display remaining time on screen
3. When time expires, declare timeout loss

TO ADD MOVE HISTORY:
1. Store moves in formatted string list
2. Display in sidebar or separate window
3. Allow clicking moves to jump to that position


================================================================================
END OF DOCUMENTATION
================================================================================
For questions or improvements, refer to the code comments throughout chess.py
Last Updated: 2026-08-18
